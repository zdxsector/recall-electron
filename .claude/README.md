# Claude Adapter

This directory contains Claude Code project-specific adapters for the canonical AI framework in `.agents/`.

Rules:

- keep agent prompts concise and repo-aware
- point back to canonical docs instead of duplicating them
- use project subagents in `.claude/agents/`
- keep durable shared knowledge in `.agents/`
- Playwright Electron testing guidance should point to the canonical testing skill
